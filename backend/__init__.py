"""
Feedback Management System backend package.

This file marks `backend` as a proper Python package so the app can be
launched from the project root with:

    py -m uvicorn backend.main:app --reload
"""
